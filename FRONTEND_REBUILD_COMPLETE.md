# ✅ Фронтенд пересобран!

## 🎉 Что сделано

1. ✅ Исправлена ошибка ESLint (`confirm` → `window.confirm`)
2. ✅ Фронтенд успешно пересобран
3. ✅ Кнопка "Загрузить кодексы РФ" добавлена в код

---

## 📋 Что нужно сделать

### Вариант 1: Если используете Docker

```bash
cd /root/advakod
docker-compose restart frontend
# или
docker-compose up -d --build frontend
```

### Вариант 2: Если используете nginx/static server

```bash
# Просто обновите страницу в браузере с очисткой кэша:
# Ctrl+Shift+R (или Cmd+Shift+R на Mac)
```

### Вариант 3: Если используете dev server

```bash
cd /root/advakod/frontend
npm start
```

---

## 🔍 Где должна быть кнопка

В разделе **"Документы"**, справа от заголовка, после кнопок:
- [Обновить] (серая)
- [Загрузить файл] (синяя)
- [Загрузить по ссылке] (зеленая)
- **[Загрузить кодексы РФ]** (фиолетовая) ← ЭТА

---

## ✅ После перезагрузки

1. Обновите страницу в браузере с очисткой кэша (`Ctrl+Shift+R`)
2. Кнопка должна появиться
3. Нажмите на неё → откроется модальное окно с опциями:
   - "Тест на одном кодексе"
   - "Загрузить все кодексы"

---

**Готово! Пересоберите фронтенд и обновите страницу.**

