#!/bin/bash

# КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ ПРОЕКТА АДВАКОД
# Этот скрипт исправляет все критические ошибки

echo "🔧 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ ПРОЕКТА АДВАКОД"
echo "=========================================="

# Остановка всех контейнеров
echo "🛑 Остановка всех контейнеров..."
docker-compose down

# Очистка volumes (ОСТОРОЖНО - удалит данные!)
echo "🧹 Очистка volumes..."
docker volume prune -f

# Пересборка образов
echo "🔨 Пересборка образов..."
docker-compose build --no-cache

# Запуск системы
echo "🚀 Запуск системы..."
docker-compose up -d

# Ожидание запуска
echo "⏳ Ожидание запуска сервисов..."
sleep 30

# Проверка статуса
echo "📊 Проверка статуса контейнеров..."
docker-compose ps

# Проверка логов backend
echo "📋 Проверка логов backend..."
docker logs advakod_backend --tail 20

echo "✅ Исправление завершено!"
echo "🌐 Откройте http://localhost для проверки"
