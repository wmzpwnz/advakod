# Работа с Git в Cursor - Полное руководство

## 🎯 Настройка Git в Cursor

### 1. Встроенные инструменты Cursor

Cursor имеет встроенную поддержку Git с визуальным интерфейсом:

**Source Control Panel (Ctrl+Shift+G):**
- Показывает все изменения
- Позволяет делать коммиты
- Встроенный diff viewer
- Staging и unstaging файлов

**Git History:**
- Визуальная история коммитов
- Возможность отката к любой версии
- Сравнение версий

### 2. Настройка Git в Cursor

Откройте Settings (Ctrl+,) и найдите секцию Git:

```json
{
  "git.enableSmartCommit": true,
  "git.confirmSync": false,
  "git.autofetch": true,
  "git.autofetchPeriod": 180,
  "git.enableCommitSigning": false,
  "git.showProgress": true,
  "git.autoStash": true
}
```

## 🔄 Откат изменений - Способы

### Способ 1: Через Source Control Panel

1. **Откройте Source Control** (Ctrl+Shift+G)
2. **Посмотрите историю** - нажмите на иконку истории
3. **Выберите коммит** для отката
4. **Правый клик** → "Reset to this commit"

### Способ 2: Через Command Palette

1. **Ctrl+Shift+P** → "Git: Reset"
2. Выберите тип reset:
   - **Soft** - сохраняет изменения в staging
   - **Mixed** - сохраняет изменения в working directory
   - **Hard** - полностью удаляет изменения

### Способ 3: Через терминал в Cursor

```bash
# Посмотреть историю
git log --oneline -10

# Откатиться на 1 коммит назад
git reset --hard HEAD~1

# Откатиться к конкретному коммиту
git reset --hard <hash>

# Создать новый коммит, отменяющий изменения
git revert <hash>

# Временно переключиться на старую версию
git checkout <hash>
```

## 🛡️ Безопасные способы отката

### 1. Revert (Рекомендуется для публичных репозиториев)

```bash
# Создает новый коммит, отменяющий изменения
git revert <hash>
git push origin master
```

**Плюсы:** Сохраняет историю, безопасно для командной работы
**Минусы:** Создает дополнительные коммиты

### 2. Reset (Для локальных изменений)

```bash
# Мягкий откат - сохраняет изменения
git reset --soft HEAD~1

# Смешанный откат - изменения в working directory
git reset --mixed HEAD~1

# Жесткий откат - полностью удаляет изменения
git reset --hard HEAD~1
```

**Плюсы:** Чистая история
**Минусы:** Может быть опасно для командной работы

### 3. Checkout (Временный просмотр)

```bash
# Временно переключиться на старую версию
git checkout <hash>

# Вернуться к master
git checkout master
```

## 🔍 Полезные команды для анализа

### Просмотр истории

```bash
# Краткая история
git log --oneline -10

# Подробная история
git log --graph --oneline --all

# История конкретного файла
git log --follow -- <filename>

# История с изменениями
git log -p <filename>
```

### Сравнение версий

```bash
# Сравнить с предыдущим коммитом
git diff HEAD~1

# Сравнить два коммита
git diff <hash1> <hash2>

# Сравнить файлы
git diff HEAD~1 -- <filename>
```

### Поиск изменений

```bash
# Найти коммит по сообщению
git log --grep="fix bug"

# Найти коммит по автору
git log --author="wmzpwnz"

# Найти коммит по дате
git log --since="2024-10-01" --until="2024-10-31"
```

## 🚨 Экстренные ситуации

### 1. Откат последнего коммита (еще не отправлен)

```bash
# Откат с сохранением изменений
git reset --soft HEAD~1

# Полный откат
git reset --hard HEAD~1
```

### 2. Откат после push

```bash
# Создать revert коммит
git revert <hash>
git push origin master
```

### 3. Восстановление удаленных файлов

```bash
# Найти коммит, где файл существовал
git log --all --full-history -- <filename>

# Восстановить файл
git checkout <hash> -- <filename>
```

### 4. Откат merge

```bash
# Откат последнего merge
git reset --hard HEAD~1

# Или создать revert
git revert -m 1 <merge-hash>
```

## 🎨 Настройка Cursor для удобной работы с Git

### 1. Расширения (рекомендуемые)

- **GitLens** - расширенная информация о Git
- **Git Graph** - визуальный граф истории
- **Git History** - детальная история файлов

### 2. Горячие клавиши

```json
{
  "key": "ctrl+shift+g",
  "command": "workbench.view.scm"
},
{
  "key": "ctrl+shift+p",
  "command": "git.commit"
},
{
  "key": "ctrl+shift+r",
  "command": "git.revert"
}
```

### 3. Настройки для удобства

```json
{
  "git.decorations.enabled": true,
  "git.decorations.colors": true,
  "git.decorations.badges": true,
  "git.showInlineOpenFileAction": true,
  "git.showProgress": true
}
```

## 📋 Чек-лист для безопасной работы

### Перед откатом:

- [ ] Убедитесь, что изменения сохранены
- [ ] Проверьте, что коммит уже отправлен на GitHub
- [ ] Создайте бэкап важных изменений
- [ ] Уведомите команду (если работаете в команде)

### После отката:

- [ ] Проверьте, что все работает корректно
- [ ] Обновите документацию при необходимости
- [ ] Создайте новый коммит с исправлениями
- [ ] Отправьте изменения на GitHub

## 🔧 Полезные алиасы Git

Добавьте в `~/.gitconfig`:

```ini
[alias]
    st = status
    co = checkout
    br = branch
    ci = commit
    unstage = reset HEAD --
    last = log -1 HEAD
    visual = !gitk
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
    undo = reset HEAD~1
    redo = reset HEAD@{1}
```

## 🎯 Практические примеры

### Пример 1: Откат последнего коммита

```bash
# Посмотреть что было в последнем коммите
git show HEAD

# Откатиться с сохранением изменений
git reset --soft HEAD~1

# Или полностью откатиться
git reset --hard HEAD~1
```

### Пример 2: Откат к конкретной версии

```bash
# Найти нужный коммит
git log --oneline -10

# Откатиться к нему
git reset --hard <hash>

# Если нужно отправить на GitHub
git push --force-with-lease origin master
```

### Пример 3: Временный просмотр старой версии

```bash
# Переключиться на старую версию
git checkout <hash>

# Посмотреть код, протестировать
# ...

# Вернуться к master
git checkout master
```

## ⚠️ Важные предупреждения

1. **--force push** может быть опасен для командной работы
2. **--hard reset** удаляет изменения безвозвратно
3. **Всегда делайте бэкапы** важных изменений
4. **Тестируйте** после отката
5. **Уведомляйте команду** о серьезных изменениях

## 🆘 Если что-то пошло не так

```bash
# Посмотреть историю операций
git reflog

# Восстановить из reflog
git reset --hard HEAD@{2}

# Или восстановить конкретный коммит
git checkout <hash>
git checkout -b recovery-branch
```

---

**Помните:** Git - это мощный инструмент, но с большой силой приходит большая ответственность! 🚀
