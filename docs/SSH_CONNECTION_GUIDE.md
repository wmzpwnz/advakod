# 🔐 Руководство по SSH-подключению к серверу АДВАКОД

**Версия:** 1.0
**Дата:** 30 ноября 2025
**Компания:** "Аврамир"

---

## 📋 Общая информация

### Доступные серверы

| Название | IP адрес | Домен | Назначение |
|----------|----------|-------|------------|
| **Production** | `31.130.145.75` | - | Основной рабочий сервер |
| **Legacy** | `89.23.98.167` | `advacodex.com` | Старый сервер |

### Учетные данные

**Production сервер:**
- **Host:** `31.130.145.75`
- **User:** `root`
- **Password:** `pG4Ju#i+i5+UPd`
- **Port:** `22` (стандартный)

---

## 🚀 Быстрый старт

### Метод 1: Использование скрипта (рекомендуется)

```bash
# Запуск интерактивного скрипта подключения
./scripts/connect_to_server.sh
```

Скрипт предложит выбрать сервер и автоматически подключится.

### Метод 2: Прямое SSH-подключение

```bash
# Подключение к production серверу
ssh root@31.130.145.75

# Введите пароль: pG4Ju#i+i5+UPd
```

### Метод 3: Использование SSH config

```bash
# 1. Скопируйте конфигурацию в домашнюю директорию
cp ssh-config ~/.ssh/config

# 2. Подключитесь через алиас
ssh advakod-server
```

---

## 🔧 Настройка SSH

### 1. Установка SSH клиента

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install openssh-client
```

**macOS:**
SSH клиент уже установлен по умолчанию.

**Windows:**
- Windows 10/11: SSH клиент встроен
- Альтернатива: [PuTTY](https://www.putty.org/)

### 2. Настройка SSH config файла

Создайте или отредактируйте файл `~/.ssh/config`:

```bash
# Создание директории (если не существует)
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Копирование конфигурации
cp ssh-config ~/.ssh/config
chmod 600 ~/.ssh/config
```

Содержимое конфигурации:

```ssh-config
Host advakod-server
    HostName 31.130.145.75
    User root
    Port 22
    ServerAliveInterval 60
    ServerAliveCountMax 3
    Compression yes
    TCPKeepAlive yes
```

### 3. Автоматический ввод пароля (опционально)

Установите `sshpass` для автоматического ввода пароля:

```bash
# Ubuntu/Debian
sudo apt-get install sshpass

# macOS
brew install hudochenkov/sshpass/sshpass

# Использование
sshpass -p 'pG4Ju#i+i5+UPd' ssh root@31.130.145.75
```

---

## 🔑 Настройка SSH ключей (рекомендуется)

SSH ключи обеспечивают более безопасный способ аутентификации без ввода пароля.

### Генерация SSH ключа

```bash
# Генерация нового SSH ключа
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"

# Следуйте инструкциям:
# 1. Нажмите Enter для сохранения в ~/.ssh/id_rsa
# 2. Введите пароль для ключа (опционально)
```

### Копирование ключа на сервер

**Метод 1: Использование ssh-copy-id**

```bash
ssh-copy-id root@31.130.145.75
# Введите пароль: pG4Ju#i+i5+UPd
```

**Метод 2: Ручное копирование**

```bash
# 1. Скопируйте содержимое публичного ключа
cat ~/.ssh/id_rsa.pub

# 2. Подключитесь к серверу
ssh root@31.130.145.75

# 3. На сервере выполните:
mkdir -p ~/.ssh
chmod 700 ~/.ssh
nano ~/.ssh/authorized_keys
# Вставьте содержимое публичного ключа
chmod 600 ~/.ssh/authorized_keys
exit

# 4. Теперь можно подключаться без пароля:
ssh root@31.130.145.75
```

### Обновление SSH config для использования ключа

Раскомментируйте строку в `~/.ssh/config`:

```ssh-config
Host advakod-server
    HostName 31.130.145.75
    User root
    Port 22
    IdentityFile ~/.ssh/id_rsa  # <-- раскомментируйте эту строку
    ServerAliveInterval 60
    ServerAliveCountMax 3
```

---

## 💻 Работа на сервере

### Базовые команды после подключения

```bash
# Проверка текущей директории
pwd

# Переход в директорию проекта
cd /opt/advakod

# Просмотр статуса Docker контейнеров
docker ps

# Просмотр логов
docker-compose -f docker-compose.prod.yml logs -f

# Проверка использования ресурсов
htop
free -h
df -h
```

### Управление проектом

```bash
# Переход в директорию проекта
cd /opt/advakod

# Остановка всех сервисов
docker-compose -f docker-compose.prod.yml down

# Запуск всех сервисов
docker-compose -f docker-compose.prod.yml up -d

# Перезапуск конкретного сервиса
docker-compose -f docker-compose.prod.yml restart backend

# Просмотр логов
docker-compose -f docker-compose.prod.yml logs -f backend
```

### Обновление кода на сервере

```bash
# 1. Подключение к серверу
ssh root@31.130.145.75

# 2. Переход в директорию проекта
cd /opt/advakod

# 3. Получение обновлений из Git
git fetch origin
git pull origin main

# 4. Перезапуск сервисов
docker-compose -f docker-compose.prod.yml down
docker-compose -f docker-compose.prod.yml up -d --build
```

---

## 🛠️ Передача файлов

### SCP (Secure Copy)

**Копирование файла на сервер:**

```bash
# Локальный файл → Сервер
scp /path/to/local/file.txt root@31.130.145.75:/opt/advakod/

# Локальная директория → Сервер (рекурсивно)
scp -r /path/to/local/dir root@31.130.145.75:/opt/advakod/
```

**Копирование файла с сервера:**

```bash
# Сервер → Локальный файл
scp root@31.130.145.75:/opt/advakod/file.txt /path/to/local/

# Сервер → Локальная директория (рекурсивно)
scp -r root@31.130.145.75:/opt/advakod/logs /path/to/local/
```

### SFTP (SSH File Transfer Protocol)

```bash
# Подключение через SFTP
sftp root@31.130.145.75

# Команды SFTP:
# ls - список файлов на сервере
# lls - список локальных файлов
# cd - изменить директорию на сервере
# lcd - изменить локальную директорию
# get file.txt - скачать файл
# put file.txt - загрузить файл
# exit - выход
```

### Rsync (для синхронизации)

```bash
# Синхронизация локальной директории с сервером
rsync -avz --progress /path/to/local/dir/ root@31.130.145.75:/opt/advakod/

# Синхронизация с сервера на локальный компьютер
rsync -avz --progress root@31.130.145.75:/opt/advakod/logs/ /path/to/local/logs/
```

---

## 🔒 Безопасность

### Рекомендации по безопасности

1. **Используйте SSH ключи вместо паролей**
   - Более безопасно
   - Удобнее (не нужно вводить пароль)

2. **Защитите приватный ключ**
   ```bash
   chmod 600 ~/.ssh/id_rsa
   ```

3. **Используйте пароль для SSH ключа**
   - Добавляет дополнительный уровень защиты

4. **Регулярно обновляйте пароли**
   - Меняйте пароли каждые 3-6 месяцев

5. **Ограничьте доступ по IP (на сервере)**
   ```bash
   # Разрешить SSH только с определенного IP
   sudo ufw allow from YOUR_IP to any port 22
   ```

### Изменение пароля на сервере

```bash
# Подключитесь к серверу
ssh root@31.130.145.75

# Измените пароль
passwd

# Введите текущий пароль
# Введите новый пароль (дважды)
```

---

## 🐛 Устранение неполадок

### Проблема: Connection timeout

```bash
# Проверка доступности сервера
ping 31.130.145.75

# Проверка открытых портов
nmap 31.130.145.75

# Проверка DNS (если используется домен)
nslookup advacodex.com
```

### Проблема: Permission denied

```bash
# Проверьте правильность пароля
# Убедитесь, что используете правильного пользователя (root)

# Проверьте права на SSH ключ
chmod 600 ~/.ssh/id_rsa
chmod 644 ~/.ssh/id_rsa.pub
chmod 700 ~/.ssh
```

### Проблема: Host key verification failed

```bash
# Удалите старый ключ хоста
ssh-keygen -R 31.130.145.75

# Или отключите проверку (не рекомендуется для продакшена)
ssh -o StrictHostKeyChecking=no root@31.130.145.75
```

### Проблема: Connection closed by remote host

```bash
# Проверьте логи на сервере (если есть доступ)
# /var/log/auth.log

# Возможные причины:
# - Неправильный пароль (несколько неудачных попыток)
# - Заблокирован IP (fail2ban)
# - SSH сервис не запущен
```

### Включение verbose режима для диагностики

```bash
# Подключение с подробными логами
ssh -v root@31.130.145.75

# Еще более подробные логи
ssh -vvv root@31.130.145.75
```

---

## 📚 Дополнительные ресурсы

### Полезные команды SSH

```bash
# Проверка версии SSH клиента
ssh -V

# Туннелирование портов (Port Forwarding)
ssh -L 8080:localhost:80 root@31.130.145.75
# Теперь localhost:8080 → server:80

# Обратное туннелирование
ssh -R 9090:localhost:3000 root@31.130.145.75
# Теперь server:9090 → localhost:3000

# Выполнение команды на сервере без интерактивного режима
ssh root@31.130.145.75 'docker ps'

# Сохранение вывода команды локально
ssh root@31.130.145.75 'docker logs backend' > local_logs.txt
```

### Работа с множественными SSH сессиями

**Использование tmux на сервере:**

```bash
# Подключиться к серверу
ssh root@31.130.145.75

# Запустить tmux
tmux

# Создать новое окно: Ctrl+b, затем c
# Переключение между окнами: Ctrl+b, затем номер окна (0-9)
# Отключиться (сессия продолжит работать): Ctrl+b, затем d
# Подключиться к существующей сессии: tmux attach
```

---

## ✅ Чек-лист первого подключения

- [ ] SSH клиент установлен
- [ ] Проверена доступность сервера (`ping 31.130.145.75`)
- [ ] Успешное подключение по паролю
- [ ] Сгенерирован SSH ключ (опционально)
- [ ] SSH ключ скопирован на сервер (опционально)
- [ ] Проверена работа без пароля (опционально)
- [ ] SSH config настроен
- [ ] Проверена работа команд на сервере
- [ ] Освоены базовые команды Docker на сервере

---

## 📞 Контакты

**Компания:** "Аврамир"
**Проект:** АДВАКОД
**Email:** support@advacodex.com
**Документация:** `/home/user/advakod/docs/`

---

**АДВАКОД - Ваш персональный ИИ-правовед 24/7!** 🏛️⚖️
