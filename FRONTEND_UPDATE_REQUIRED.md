# 🔄 Обновление фронтенда требуется!

## ⚠️ Проблема

Кнопка "Загрузить кодексы РФ" добавлена в код, но может не отображаться, если фронтенд не пересобран.

---

## ✅ Решение

### Вариант 1: Пересобрать фронтенд (если используете production build)

```bash
cd /root/advakod/frontend
npm run build
```

### Вариант 2: Если используете dev server - перезапустить

```bash
cd /root/advakod/frontend
npm start
```

### Вариант 3: Hard refresh в браузере

1. Откройте админку
2. Нажмите `Ctrl+Shift+R` (или `Cmd+Shift+R` на Mac)
3. Это обновит страницу с очисткой кэша

---

## 📍 Где должна быть кнопка

В разделе **"Документы"**, в верхней части, справа от заголовка:

```
[Обновить] [Загрузить файл] [Загрузить по ссылке] [Загрузить кодексы РФ] ← ЭТА
```

Кнопка **фиолетовая** (`bg-purple-600`)

---

## 🔍 Проверка

Кнопка находится в коде на строках 472-478 файла:
`/root/advakod/frontend/src/components/DocumentManagement.js`

Если кнопки нет после обновления:
1. Проверьте консоль браузера (F12) на ошибки
2. Убедитесь, что файл сохранен
3. Пересоберите фронтенд

