# 🔄 Руководство по восстановлению проекта ADVAKOD

Этот документ описывает полное восстановление проекта из Git репозитория.

## 📋 Предварительные требования

- Ubuntu 20.04+ или Debian 11+
- Docker и Docker Compose установлены
- Минимум 32GB RAM (для Vistral 24B модели)
- Минимум 100GB свободного места на диске
- Доступ к домену с настроенным DNS

## 🚀 Быстрое восстановление

### 1. Клонирование репозитория

```bash
git clone <your-repo-url> advacodex
cd advacodex
```

### 2. Настройка переменных окружения

```bash
# Создайте файл env.production из шаблона
cp env.production.template env.production

# Откройте файл и заполните все значения
nano env.production
```

**Критически важные параметры для заполнения:**

```bash
# Сгенерируйте SECRET_KEY
python3 -c "import secrets; print('SECRET_KEY=\"' + secrets.token_urlsafe(64) + '\"')"

# Сгенерируйте ENCRYPTION_KEY
python3 -c "import secrets; print('ENCRYPTION_KEY=\"' + secrets.token_urlsafe(32) + '\"')"

# Установите безопасный пароль для PostgreSQL
POSTGRES_PASSWORD="ваш_безопасный_пароль"
```

### 3. Настройка SSL сертификатов

```bash
# Установите certbot
sudo apt-get update
sudo apt-get install certbot

# Получите сертификат для вашего домена
sudo certbot certonly --standalone -d advacodex.com -d www.advacodex.com

# Сертификаты будут в:
# /etc/letsencrypt/live/advacodex.com/fullchain.pem
# /etc/letsencrypt/live/advacodex.com/privkey.pem
```

### 4. Загрузка AI модели

```bash
# Создайте директорию для моделей
sudo mkdir -p /opt/advakod/models/vistral
sudo chown -R $USER:$USER /opt/advakod

# Загрузите модель Vistral 24B
# Используйте скрипт или загрузите вручную
bash 2_download_models.sh

# Или вручную:
cd /opt/advakod/models/vistral
# Загрузите Vistral-24B-Instruct-Q5_0.gguf
```

### 5. Инициализация базы данных

```bash
# Запустите только PostgreSQL для инициализации
docker-compose -f docker-compose.prod.yml up -d postgres

# Подождите пока база запустится (30 секунд)
sleep 30

# Примените миграции
docker-compose -f docker-compose.prod.yml exec backend alembic upgrade head

# Или если backend еще не запущен:
cd backend
docker run --rm \
  --network advacodex_advakod_default \
  -v $(pwd):/app \
  -w /app \
  advakod-backend:latest \
  alembic upgrade head
```

### 6. Создание администратора

```bash
# Запустите backend
docker-compose -f docker-compose.prod.yml up -d backend

# Создайте суперпользователя
docker-compose -f docker-compose.prod.yml exec backend python create_admin.py
```

### 7. Запуск всех сервисов

```bash
# Запустите все сервисы
docker-compose -f docker-compose.prod.yml up -d

# Проверьте статус
docker-compose -f docker-compose.prod.yml ps

# Проверьте логи
docker-compose -f docker-compose.prod.yml logs -f
```

### 8. Восстановление данных (если есть бэкапы)

```bash
# Восстановление PostgreSQL
docker-compose -f docker-compose.prod.yml exec postgres pg_restore \
  -U advakod -d advakod_db < backups/postgres_backup.sql

# Восстановление Qdrant (если есть бэкап)
# Скопируйте данные в volume qdrant_data
```

## 📦 Что восстанавливается из Git

✅ **Восстанавливается автоматически:**
- Весь код проекта (backend, frontend)
- Конфигурация Docker (docker-compose.prod.yml)
- Конфигурация Nginx (nginx.strict.conf)
- Шаблоны переменных окружения
- Скрипты развертывания
- Документация

❌ **НЕ восстанавливается (нужно настроить вручную):**
- Секреты и пароли (SECRET_KEY, ENCRYPTION_KEY, POSTGRES_PASSWORD)
- SSL сертификаты
- AI модели (нужно загрузить отдельно)
- Данные базы данных (PostgreSQL, Qdrant, Redis)
- Загруженные файлы пользователей

## 🔧 Дополнительная настройка

### Настройка автоматических бэкапов

```bash
# Скрипт бэкапа уже включен в docker-compose.prod.yml
# Проверьте что сервис backup запущен
docker-compose -f docker-compose.prod.yml ps backup
```

### Настройка мониторинга

```bash
# Опционально: запустите мониторинг
cd backend/monitoring
docker-compose -f docker-compose.monitoring.yml up -d
```

### Настройка домена

Убедитесь что DNS записи настроены:
- A запись для advacodex.com → IP сервера
- A запись для www.advacodex.com → IP сервера

## ✅ Проверка работоспособности

```bash
# Проверка здоровья сервисов
curl https://advacodex.com/health

# Проверка API
curl https://advacodex.com/api/v1/health

# Проверка WebSocket
# Откройте https://advacodex.com в браузере
```

## 🆘 Решение проблем

### Проблема: База данных не подключается

```bash
# Проверьте что PostgreSQL запущен
docker-compose -f docker-compose.prod.yml ps postgres

# Проверьте логи
docker-compose -f docker-compose.prod.yml logs postgres

# Проверьте переменные окружения
docker-compose -f docker-compose.prod.yml exec backend env | grep POSTGRES
```

### Проблема: Модель не найдена

```bash
# Проверьте путь к модели
ls -lh /opt/advakod/models/vistral/

# Проверьте права доступа
sudo chown -R $USER:$USER /opt/advakod/models
```

### Проблема: SSL сертификат не работает

```bash
# Проверьте что сертификаты существуют
sudo ls -la /etc/letsencrypt/live/advacodex.com/

# Обновите сертификаты
sudo certbot renew
```

## 📝 Чеклист восстановления

- [ ] Репозиторий склонирован
- [ ] env.production создан и заполнен
- [ ] SECRET_KEY и ENCRYPTION_KEY сгенерированы
- [ ] POSTGRES_PASSWORD установлен
- [ ] SSL сертификаты получены
- [ ] AI модель загружена
- [ ] База данных инициализирована
- [ ] Миграции применены
- [ ] Администратор создан
- [ ] Все сервисы запущены
- [ ] DNS настроен
- [ ] Сайт доступен по HTTPS
- [ ] API работает
- [ ] WebSocket работает

## 🔐 Безопасность

После восстановления обязательно:

1. **Измените все пароли** (особенно если они были в бэкапах)
2. **Проверьте права доступа** к файлам:
   ```bash
   chmod 600 env.production
   ```
3. **Настройте firewall**:
   ```bash
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   sudo ufw enable
   ```
4. **Настройте автоматическое обновление SSL**:
   ```bash
   sudo certbot renew --dry-run
   ```

## 📞 Поддержка

Если возникли проблемы при восстановлении:
1. Проверьте логи: `docker-compose -f docker-compose.prod.yml logs`
2. Проверьте документацию в `/docs`
3. Проверьте issues в репозитории

---

**Версия документа:** 1.0.0  
**Дата обновления:** 2024-11-14

