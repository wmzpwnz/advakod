# Развертывание режимов чата (Базовый/Эксперт)

## ✅ Реализация завершена

Все компоненты реализованы и готовы к работе.

## 🚀 Шаги для запуска

### 1. Применить миграцию БД

```bash
cd /var/www/advacodex.com/backend
alembic upgrade head
```

Это добавит поле `chat_mode` в таблицу `chat_sessions`.

### 2. Перезапустить backend

```bash
# Если используется systemd
sudo systemctl restart advacodex-backend

# Или если запущен вручную
# Остановить текущий процесс и запустить заново
```

### 3. Перезапустить frontend (если нужно)

```bash
cd /var/www/advacodex.com/frontend
npm run build  # Если используется production build
# Или просто перезапустить dev сервер
```

## 📋 Что было реализовано

### Backend:
- ✅ Миграция БД для поля `chat_mode`
- ✅ Модель `ChatSession` с полем `chat_mode`
- ✅ Схемы с поддержкой режимов
- ✅ `unified_llm_service.create_legal_prompt()` с режимами
- ✅ Все эндпоинты чата используют режимы
- ✅ Новый эндпоинт `PATCH /chat/sessions/{id}/mode`
- ✅ Кеширование с учетом режима

### Frontend:
- ✅ Загрузка режима из сессии
- ✅ Отправка режима в API
- ✅ Красивое уведомление при переключении
- ✅ UI для выбора режима

## 🎯 Как использовать

1. **Переключение режима**: Нажмите на кнопку настроек (шестеренка) рядом с полем ввода
2. **Выбор режима**: Выберите "Базовый" или "Эксперт"
3. **Уведомление**: Появится красивое уведомление об изменении режима
4. **Применение**: Режим сохраняется в сессии и применяется ко всем последующим сообщениям

## 🔧 Конфигурация режимов

Режимы настраиваются в `/var/www/advacodex.com/backend/app/api/chat.py`:

```python
CHAT_MODE_CONFIG = {
    "basic": {
        "temperature": 0.3,
        "top_p": 0.8,
        "max_tokens": 3000,
    },
    "expert": {
        "temperature": 0.5,
        "top_p": 0.9,
        "max_tokens": 5000,
    }
}
```

## ⚡ Производительность

- ✅ **Не влияет на скорость**: Режим определяется до начала генерации
- ✅ **Работает со стримингом**: Режим передается в промпт до начала генерации
- ✅ **Кеширование**: Раздельные кеши для разных режимов

## 🐛 Проверка работы

1. Откройте чат
2. Нажмите на кнопку настроек (шестеренка)
3. Выберите режим "Эксперт"
4. Должно появиться уведомление "✅ Режим изменен"
5. Отправьте сообщение - ответ должен быть в экспертном режиме

## 📝 Примечания

- Старые сессии автоматически получают режим 'basic'
- Режим сохраняется в сессии и применяется ко всем сообщениям
- Можно переключать режим в любой момент

